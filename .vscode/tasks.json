{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build with CMake",
            "type": "shell",
            "command": "cmake -S . -B build && cmake --build build",
            "problemMatcher": [],
            "group": "build"
        },
    ]
}
